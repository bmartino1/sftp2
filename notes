WIP as of 9/9/25

Base docker image:
Debian 12 -slim image

log durring docker build:
Fail2Ban: 1.0.2
OpenSSH client: 9.2p1
OpenSSH server: 1:9.2p1-2+deb12u7
whois: 5.5.17
glibc: 2.36
Built at: 2025-09-10T03:22:41Z

Docker Run:
Reading state information...
openssh-server is already the newest version (1:9.2p1-2+deb12u7).
openssh-client is already the newest version (1:9.2p1-2+deb12u7).
openssh-sftp-server is already the newest version (1:9.2p1-2+deb12u7).
fail2ban is already the newest version (1.0.2-2).
whois is already the newest version (5.5.17).

WIP-
ssh not runnign as it should

docker console and run to start the missing sshd task for sftp

nohup /usr/sbin/sshd -e -D -f /etc/ssh/sshd_config >/var/log/sshd.foreground.log 2>&1 &
sleep 1
pgrep -a sshd
ss -ltnp | grep ':22' || true

WIP - fail2ban is working as it should though...
need to readded teh disaply loggin to the docker logs... login is workign other wise...
